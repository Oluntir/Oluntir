'use strict';const assert=require('assert');const a=require('../core/static-script-analyzer.js');const r=a.analyze("import x from './x.js'; document.querySelector('.a').addEventListener('click',()=>el.classList.toggle('open')); new MutationObserver(()=>{});",{file:'app.js'});const kinds=r.references.map(x=>x.kind);assert(kinds.includes('module-import'));assert(kinds.includes('dom-selector'));assert(kinds.includes('event-listener'));assert(kinds.includes('class-manipulation'));assert(kinds.includes('observer'));console.log('STATIC-SCRIPT-ANALYZER-TEST ERFOLGREICH');
