# Testing

Every contract change requires a focused test and the existing regression suite. A test result must distinguish new regressions from known historical test assumptions.

## Required behavior

- State assumptions explicitly.
- Preserve existing contracts unless the task requires a versioned change.
- Update tests with code.
- Do not document unimplemented behavior.
