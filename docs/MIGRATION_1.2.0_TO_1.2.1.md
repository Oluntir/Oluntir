# Migration from Oluntir 1.2.0 to 1.2.1

Back up the project, extract 1.2.1 into a new directory, open the `.oluntir` file, and save explicitly after review. Missing internal layout IDs are added without changing Shared Content, OPE Includes, HTML IDs, classes, text, images, or links. Reopening the project does not create additional IDs or structures. Test HTML, SSI, and PHP export before replacing the previous working installation.
